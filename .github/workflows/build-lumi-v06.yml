name: Build Lumi v0.6 APK

on:
  push:
    branches: ["lumi-v06-build"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Reconstruct and extract source
        run: |
          base64 -d lumi-build-v06/source.b64.txt > /tmp/LumiVoiceBridge-v0.6-source.zip
          unzip -t /tmp/LumiVoiceBridge-v0.6-source.zip
          rm -rf lumi-v06-src
          mkdir lumi-v06-src
          unzip -q /tmp/LumiVoiceBridge-v0.6-source.zip -d lumi-v06-src

      - name: Build debug APK
        working-directory: lumi-v06-src/LumiVoiceBridge-v0.6
        run: gradle --no-daemon assembleDebug

      - name: Prepare APK
        run: |
          cp lumi-v06-src/LumiVoiceBridge-v0.6/app/build/outputs/apk/debug/app-debug.apk LumiVoiceBridge-v0.6-debug.apk
          sha256sum LumiVoiceBridge-v0.6-debug.apk > LumiVoiceBridge-v0.6-debug.apk.sha256

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LumiVoiceBridge-v0.6-APK
          path: |
            LumiVoiceBridge-v0.6-debug.apk
            LumiVoiceBridge-v0.6-debug.apk.sha256
          if-no-files-found: error
          retention-days: 7
